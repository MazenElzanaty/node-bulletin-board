pipeline {
    agent {
        docker { dockerfile true }
    }
    stages {
        stage('Change') {
            steps {
                sh change.sh
            }
        }
        stage('Build') {
            steps {
                sh 'node --version'
                sh 'npm install'
                
            }
        }
    }
}